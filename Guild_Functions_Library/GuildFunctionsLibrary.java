public class Guild_Functions_Library {
    public static double sum(double[] 
numbers) {
    double total = 0;
    for (int i = 0; i < 
        numbers.lenght; i++)
        total +=numbers [i];
    }
    return total;
    }
public static double average(double[]
    numbers) {
        return sum(numbers) /
    numbers.length;
    }
    public static double max(double[]
        numbers) {
            doubke maxValue = numbers[0];

}

